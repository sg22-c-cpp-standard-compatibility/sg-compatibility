# May 15th, 2026
## Agenda

[P3677 R0 Preseving LC_TYPE at program start for UTF-8 locales](https://github.com/sg22-c-cpp-standard-compatibility/sg-compatibility/issues/67)
[P3953 R1 Rebasing <cmath> on C23](https://github.com/cplusplus/papers/issues/2589)

## Attendees

* Aaron Ballman (AB)
* Corentin Jabot (CJ) [Author]
* Davis Herring (DH)
* Hubert Tong (HT)
* Jan Schultke (JS) [Assistant Chair] [Author]
* JeanHeyd Meneide (JHM) [Scribe]
* Jens Maurer (JM)
* Joshua Cranmer (JC)
* Matthias Wippich (MW)
* Maryam Karampour (MK)
* Tom Honermann (TH)
* Timur Doumler (TD) [Chair]
* Rajan Bhakta (RB)
* Robert Seacord (RCS)
* Vlad Serebrennikov (VS)


SCRIBE NOTE: General meeting Procedure to start, Timur Doumler introduces himself as the newest Chair. Just getting acclimated.

## P3677r0 - https://www.open-std.org/jtc1/sc22/wg21/docs/papers/2025/p3677r0.pdf
Corentin Jabot
Time: 19:38:28
CJ: (Explains premise of paper described at the start of the paper, explains that C controls the locale, that this is really a WG14 paper mostly.)
CJ: (Describes that every implementation sets the locale to be the default "C" locale to start, by doing the equivalent of `setlocale(LC_ALL, "C")`.)

(SCRIBE NOTE: We lost Corentin Jabot in the middle of his explanation. Power outage. We shared horror stories of Zoom and Microsoft Teams crashes, huddled around the softly glowing screens while we wait to reunite.)
(Scribe note: Corentin came back, presentation resumes.)
Time: 19:44:54.

CJ: (Resumes at the descrption of UTF-8 locale not being selected when the default locale `"C"` is picked, and that most implementations have a UTF-8 locale packed into the operating system. We want to change the call at startup to try to use the UTF-8, and then fall back to the default `"C"` locale. This is important because the locale and encoding are tied together and get set thanks to the locale setting, so we want a better chance to startup with UTF-8. We specifically want to affect encoding.)
JHM: Pointing out that this was seen by WG14, and it didn't quite make it through. The primary hangup seemed to be that this compelled a potential behavior change from the wording since it asked for what might be a change in the default locale based on what the environment locale (`setlocale(... ,"")`) might do. This could maybe impact e.g. an EBCDIC program or ASCII locale having its default C locale changed to UTF-8.
CJ: Right. But it's not really a problem because we are only asking for a specific string that changes the encoding and the rest of the monetary symbol or decimal symbols or padding symbols are meant to be kept the same, and the wording does that. (Further explanation, also points out Rust and Python are now doing the UTF-8 setup by-default at their startup.)
RB: Firstly, we are saying "EBDIC" a lot, but there are plenty of other locales that this affects. Secondly, it's not necessarily that I am against the change, but my worry is that this will break programs. For example, a C program that works today -- right now -- and has worked for the last 40 years under these conditions might change and that could introduce loud breaks if we are lucky or worse breaks that quietly change data. And that can be bad and produce issues.
CJ: Locales like Shift-JIS and so on, right, are also affected by this sort of thing. But what we are hoping for is that because it only changes encoding and not any of the other symbols, most things like printing and what not will not be affected, and it will match what other programs like Rust or Python3 are doing today.
RB: And it's fine for Rust or Go or Python to do that but even when they call those programs they work in the default locale -- the "C" locale, and that assumption is how they work.
CJ: So, let me ask this. How do the environments you work in handle when a program is invoked as a child process from Python or something? Because these ones have environments that are UTF-8 and were started up as UTF-8  now.
RB: Well they handle it from `fork` or `exec` a program, and it works in the default C locale which may be EBCDIC or ASCII or something and not the same as the Rust or Go program which is ASCII or UTF-8 or something. On these platforms it is ASCI. But what happens is that the native C program, the child program, it does conversions and then passes the data back while working in the default C locale.
(Scribe Note: Back and forth between Rajan and Corentin about this point and how these programs work and that UTF-8 is the default of Rust and Python these days for UTF-8. JeanHeyd, while scribing, puts something in the chat, and it's brought up later...)
HT: (Asking a question to Rajan Bhakta): Is it possible to have two-locales in flight at the same time? So, for example, if you set the locale to EBCDIC, is it still possible to access the ASCII prtinf and utilities?
HT: It's worthy to put this chat message in the minute, because it's possible for the child process to be different from the paren't process.
JHM: (Scribe Note: shared in the chat, reproduced below:)

> I think Corentin is assuming the C program gets the parent's locale/encoding, but that's not true. The C program when called from Python/Rust/Go/etc. gets the default locale.
>
> (The default locale doesn't have to be the same as the  `fork` /  `exec`  /  `etc.` -ing program)

TH: Hubert you mentioned a program that would break if `MB_LEN_MAX` changes. Can you give an example of that?
HT: Sure, I can give an example, but later because I have a lot a work to do. (Scribe Note: describes working on stuff but that work spawning more work, effectively making "Negative Progress".)
RCS: (Scribe Note: Shared in the Chat, reproduced below:)

> `mblen` / `mbtowc` / `wctomb` — These now interpret multi-byte sequences as UTF-8 instead of single-byte. A byte like 0xC0 that was previously an invalid-but-single-byte character is now treated as a UTF-8 lead byte expecting a continuation.
> `mbstowcs` / `wcstombs` — Buffer size calculations based on the assumption that 1 byte = 1 character break silently. Code like:
> 

```cpp
// "Safe" buffer: same size as input
wchar_t wbuf[strlen(input) + 1];
mbstowcs(wbuf, input, strlen(input) + 1);
```

> This still works (the wide buffer is large enough), but the reverse pattern — allocating wcslen(ws) + 1 bytes for wcstombs output — can now overflow because one wide character can become up to 4 bytes.

(More technical discussion about how things may change or break, and the actual **SCOPE** of the changes or breaks and whether or not it is acceptable and how different combinations of parent/child programs work.)

TD: Okay, we've run over time and we need to take some polls. We're going to just do this one.

POLL: We recommended for WG14 to adopt P3677R0: Preserving LC_CTYPE at program start for UTF-8 locales.
WG14 Delegation
SF F N A SA
 1 3 2 0  1
Result: weak consensus

WG21 Delegation
SF F N A SA
 1 2 5 0  0
Result: not consensus


Time: 20:22:45

Voting Rationale Notes:

JM: This is an advisory group, so the better way to interpret this poll is to see the numbers and see what will help increase consensus in the respective committees.
TH: We seemed to have a lot of abstentions, is there something they want to say that will help with that?
TD: Yeah, I'm curious about that because one person said about abstentions and strongly against.
(Strongly Against Person): I don't think I can support this unless there is a significant change in design.
(Neutral Person): Do not care.
(Neutral Person): Specific examples of functions changing behavior (e.g. from the chat) and specific examples of python programs spawning C or C++ processes and explicitly doing conversions inbetween. So I would really like to see concrete examples about how things change and how it bites you and what are the mitigating.

 -------------

## P3953r1 - https://www.open-std.org/jtc1/sc22/wg21/docs/papers/2026/p3935r1.html
Jan Schultke
Time: 20:29:25

JS: (Explains the rationale of the paper, references the table in the paper that gives an overview of all the things normal functions change.)
JS: A specific oddity is the things that come from `<tgmath.h>` and might need template replacements in C++ for `<cmath>`. This stuff is implemented using a generic selection macro or an intrinsic because that stuff doesn't exist in C++, so we change it to templates in C++ to make these fully generic.
JS: Another specific oddity is the existence of functions that only come from C23 Annex F, which are useful and occasionally have functions that only work for specific extended floating point types that are not part of the usual `float`/`double`/`etc.`.
JS: (Describes a design change to remove the type-specific functions and instead fully rely on the templated functions, with a `#define` shim to replace functionality that is type-specific for C such as `#define f32add(...) ::std::fadd<std::float32_t>(__VA_ARGS__)`.)
TD: I see that some of this looks different from what was sent in the schedule / invitation... what is going on here?
JS: I had finished an R1 of this paper after the schedule was sent out. (Scribe Note: author reviews changes between r0 and r1 and shows the Revision 1 Changelog at the top of the paper.)
TD: Okay, I get the differences. Thank you.
JS: (Continues presenting what was going on; shows a lot of the newly added functions based on the new types such as adding `constexpr`, that there's no `noexcept` (one of the R1 changes from R0 since SG6 - Numerics has seen the paper), and generic forms of the more advanced functions like `sinpi` that are being added. Gets to the wording part and describes some of it.)
TD: We don't need to review wording here, exactly, other groups are good at that. We just need to review the design so we can say we support it or not.
JS: Yeah, so I have some... questions. Like, is it possible to do make functions conditionally-available based on `std::numeric_limits<T>::is_iec559`.
CJ: So you said this was implemented in C, and I get that, but adding all of these functions as `constexpr` is far beyond what is just making the C feature available. We will probably also mention this in other groups. (Scribe note: other WG21 groups.) This will make it difficult for some C++ implementations to adjust to having these functions. So I'd be more comfortable with not making it `constexpr` but still adding the functions.
JS: I'm sorry, aren't these all just mapped to intrinsics?
CJ: Yeah, but the thing is pretty much every implementation except GCC.
JM: I would like to voice some opposition against the templated `fadd` and `dadd` functions and that family. The first letter does indicate the result type, and I am not exactly sure that we'll get compatible call signatures when people give (for exmaple) explicit template arguments to these functions. These could have different call signatures for C versus C++ for `fadd` depending on the arguments, so I suggest that we be prudent in this first round and not do the generic return type functions.
(Scribe Note, from CJ: "If Erich or JF were there, I'm sure they would have a few `dadd` jokes.")
(JS and JM discuss the implementation details of these functions, and JS realizes that the underlying implementation might not work out as cleanly with the function templates with the type-generic function. They agree it would reduce the attack surface for this paper.)
RB: So the reason we have these be specific functions and then had them as macros is because passing function to other places made it possible for the function to raise signals when called, and preserved it. Another thing that is different is rather than just keying off `is_iec559`, the user has to specify a `WANT_` macro to get them. I'm not saying this difference is bad between C and C++, but just that it is different.
RCS: 
JS: Yeah do not do compile-time floating point exception handling.
TD: So if it does appear in a constant expression, you just get an error...?
JS: Yeah you get a compiler error. It's like dividing by zero.
JC: Just a comment about the `constexpr` functions. Yes, adding all of these `constexpr` functions are painful for compilers, but if you already support the regular `float`, `double`, `long double` stuff, it's not really much extra work to do the rest of the intrinsics. So since we already added that stuff from the first wave of `constexpr <cmath>` it's not really that much more difficult to do the rest of it.
TD: Okay... I think that's eveyone. Oh, wait, we have a comment from Jens Maurer?
JM: Yeah, just, thanks for pointing out how Signaling NaNs and whatnot are handled. I don't think we have wording in C++ to handle it in generally, and the specifics.
DH: I actually think this was briefly SG6 - Numeric, and it's not really settled because there's some discussion is trying to handle some of this.
HT: Just want to point out that this group is more equipped with implementers and exotic platforms of `constexpr`ification for these `<cmath>` functions because it's probably 

POLL: We recommended for WG21 to adopt P3935R1: Rebasing `<cmath>` on C23, assuming the templates (like `fadd`) are removed.
WG14 Delegation
SF F N A SA
 1 3 0 1  0
Result: Consensus.

WG21 Delegation
SF F N A SA
 2 4 1 0  0
Result: Consensus.

Rationale Notes:
(Against Person): Reduction of differences with C (change to having a `WANT_` macro or some similar mechanism and then handling of Signaling NaNs.)
